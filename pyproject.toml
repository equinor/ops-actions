[project]
name = "ops-actions"
version = "9.42.1"
description = "Reusable GitHub Actions workflows for common operational tasks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "click==8.5.0",
    "mkdocs-material>=9.7.7",
]
