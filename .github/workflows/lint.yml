name: Lint Codebase

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  lint-actions:
    name: Lint actions
    permissions:
      contents: read
      security-events: write
      actions: read
    uses: ./.github/workflows/zizmor-codeql.yml
